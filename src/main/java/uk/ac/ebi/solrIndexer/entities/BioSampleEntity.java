package uk.ac.ebi.solrIndexer.entities;

public class BioSampleEntity {
	/*	
	DataBaseConnection dbi = new DataBaseConnection();
	
	public List<BioSample> fetchSamples() {
		log.debug("fetchSamples()");

		CriteriaBuilder criteria = manager.getCriteriaBuilder();
		CriteriaQuery<BioSample> querySample = criteria.createQuery(BioSample.class);
		List<BioSample> samples = manager.createQuery(querySample.select(querySample.from(BioSample.class))).getResultList();
		return samples;
	}
	

	public List<BioSampleGroup> fetchGroups() {
		log.debug("fetchGroups()");

		CriteriaBuilder criteria = manager.getCriteriaBuilder();
		CriteriaQuery<BioSampleGroup> queryGroup = criteria.createQuery(BioSampleGroup.class);
		List<BioSampleGroup> groups = manager.createQuery(queryGroup.select(queryGroup.from(BioSampleGroup.class))).getResultList();
		return groups;
	}

	public List<BioSample> fetchSamples() {
		log.debug("fetchSamples()");

		CriteriaBuilder criteria = manager.getCriteriaBuilder();
		CriteriaQuery<BioSample> querySample = criteria.createQuery(BioSample.class);
		List<BioSample> samples = manager.createQuery(querySample.select(querySample.from(BioSample.class))).getResultList();
		return samples;
	}

	public Collection<ExperimentalPropertyValue> fetchExperimentalPropertyValues(BioSample bs){
		return bs.getPropertyValues();
	}
	*/
}
